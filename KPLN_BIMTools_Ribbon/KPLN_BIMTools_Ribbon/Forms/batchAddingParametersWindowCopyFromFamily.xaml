<Window x:Class="KPLN_BIMTools_Ribbon.Forms.batchAddingParametersWindowCopyFromFamily"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
        mc:Ignorable="d" 
        FontFamily="Monaco, Consolas, 'Andale Mono', 'DejaVu Sans Mono', monospace" 
        FontSize="14"
        Title="KPLN: Пакетное копирование параметров из одного семейства в другие" 
        WindowStyle="ToolWindow" 
        Topmost="True" 
        WindowStartupLocation="CenterScreen" 
        Width="1000" 
        Height="455" 
        ResizeMode="NoResize"
        Background="#FF292D36">
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="0" Margin="0,0,0,10">
            <TextBlock Text="Выберите семейство-донор:" Foreground="White" Margin="0,0,0,5" FontSize="11" />
            <ComboBox x:Name="CB_familyName" SelectionChanged="CB_familyName_SelectionChanged"/>
        </StackPanel>

        
        
        
        
        
        <Grid Grid.Row="1" Margin="0,0,0,10" Background="#FF3C414C">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>
            <Grid Grid.Row="0" Margin="0,0,0,5">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="470px"/>
                    <ColumnDefinition Width="195px"/>
                    <ColumnDefinition Width="150px"/>
                    <ColumnDefinition Width="150px"/>
                </Grid.ColumnDefinitions>
                <TextBlock Text="Параметр" Height="20px" Padding="10 4 0 0" Foreground="White" Background="#FF24272D" FontWeight="Bold" FontSize="12" />
                <TextBlock Text="Значение параметра" Height="20px" Padding="2 4 0 0" Grid.Column="1" Foreground="White" Background="#FF24272D" FontWeight="Bold" FontSize="12" />
                <TextBlock Text="Копировать значение" Height="20px" Padding="10 4 0 0" Grid.Column="2" Foreground="White" Background="#FF24272D" FontWeight="Bold" FontSize="12" />
                <TextBlock Text="Копировать параметр" Height="20px" Padding="10 4 0 0" Grid.Column="3" Foreground="White" Background="#FF24272D" FontWeight="Bold" FontSize="12" />
            </Grid>
            <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto">
                <StackPanel x:Name="ParameterListPanel" />
            </ScrollViewer>
        </Grid>
        
        
        
      
        <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Right">
            <Button Content="Выбрать семейства и копировать параметры" Width="350" Margin="0,0,0,0" Click="ButtonNextStep_Click"/>
        </StackPanel>
    </Grid>
</Window>
