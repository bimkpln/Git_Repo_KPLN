<Window x:Class="KPLN_BIMTools_Ribbon.Forms.ConfigDispatcher"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
        mc:Ignorable="d" 
        FontFamily="Monaco, Consolas, 'Andale Mono', 'DejaVu Sans Mono', monospace" 
        FontSize="14"
        ResizeMode="CanResizeWithGrip" 
        Title="KPLN: Диспетчер конфигураций экспорта" 
        WindowStyle="ToolWindow" 
        Topmost="True" 
        WindowStartupLocation="CenterScreen" 
        SizeToContent="Width"
        Height="500" 
        MinWidth="600" 
        Background="#FF292D36">
    <Window.Resources>

        <BooleanToVisibilityConverter x:Key="BoolToVis" />

        <Style 
            x:Key="UserActionBtns"
            TargetType="Button">
            <Setter Property="Padding" Value="5,5"/>
            <Setter Property="Margin" Value="5"/>
            <Setter Property="BorderBrush" Value="Green"/>
            <Setter Property="BorderThickness" Value="2"/>
            <Setter Property="Background" Value="White"/>
            <Setter Property="FontWeight" Value="Bold"/>
            <Setter Property="FontSize" Value="12"/>
            <Setter Property="IsEnabled" Value="False"/>
        </Style>

    </Window.Resources>

    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" MinHeight="20"/>
            <RowDefinition Height="Auto" MinHeight="20"/>
            <RowDefinition/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!--Фильтр-->
        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="0.2*"/>
                <ColumnDefinition Width="0.7*"/>
            </Grid.ColumnDefinitions>
            <TextBlock 
                Text="Фильтр по имени:" 
                Margin="0,0,0,5" 
                TextWrapping="Wrap"
                FontWeight="Bold" 
                FontSize="14"
                Foreground="White"/>
            <TextBox 
                x:Name="FilterItems"
                Grid.Column="1"
                TextWrapping="Wrap"
                HorizontalContentAlignment="Stretch"
                TextAlignment="Left"
                TextChanged="FilterItems_TextChanged"/>
        </Grid>
        <!--Фильтр-->
        
        <!--Заголовок-->
        <TextBlock 
            Grid.Row="1"
            Text="Доступные конфигурации:" 
            TextWrapping="Wrap"
            Margin="0,0,0,5" 
            FontWeight="Bold" 
            FontSize="14"
            Foreground="White"/>
        <!--Заголовок-->

        <!--Блок с набором конфигов-->
        <ScrollViewer 
            Padding="0,0,5,0" 
            Grid.Row="2" 
            HorizontalScrollBarVisibility="Disabled" 
            VerticalScrollBarVisibility="Auto">
            <StackPanel Orientation="Vertical" x:Name="sp">
                <ItemsControl 
                    x:Name="iControllGroups" 
                    ItemsSource="{Binding CurrentDBRevitDocExchanges}">
                    <ItemsControl.ItemTemplate>
                        <DataTemplate>
                            <Grid Margin="10">
                                <Grid.RowDefinitions>
                                    <RowDefinition/>
                                </Grid.RowDefinitions>

                                <Rectangle 
                                    Margin="-5" 
                                    RadiusX="3" 
                                    RadiusY="3" 
                                    Fill="White" 
                                    Stroke="AliceBlue"
                                    StrokeThickness ="2"/>

                                <StackPanel Orientation="Horizontal" Grid.Row="0">
                                    <CheckBox
                                        x:Name="DocExchangeChBx"
                                        IsChecked="False"
                                        Checked="OnConfigChecked"
                                        Unchecked="OnConfigUnChecked"/>
                                    <TextBlock 
                                        TextWrapping="Wrap">
                                        <TextBlock.ToolTip>
                                            <StackPanel MaxWidth="300">
                                                <TextBlock 
                                                    TextWrapping="Wrap" 
                                                    FontSize="10"
                                                    Text="Нажми ПКМ, чтобы редактировать, копировать или удалить данную конфигурацию"/>
                                            </StackPanel>
                                        </TextBlock.ToolTip>
                                        <TextBlock.ContextMenu>
                                            <ContextMenu>
                                                <MenuItem 
                                                    Header="Изменить" 
                                                    Click="MenuItem_Update_Click"/>
                                                <MenuItem 
                                                    Header="Копировать" 
                                                    Click="MenuItem_Copy_Click"/>
                                                <MenuItem 
                                                    Header="Удалить" 
                                                    Click="MenuItem_Delete_Click"/>
                                            </ContextMenu>
                                        </TextBlock.ContextMenu>
                                        <TextBlock.Inlines>
                                            <Run Text="{Binding SettingName}"/>
                                            
                                            <LineBreak/>
                                            <Run FontSize="10" 
                                                Foreground="Gray" 
                                                FontWeight="Bold"
                                                Text="Путь для сохранения:"/>
                                            <LineBreak/>
                                            <Run FontSize="10" 
                                                Foreground="Gray" 
                                                Text="{Binding SettingResultPath}"/>
                                            
                                            <LineBreak/>
                                            <Run FontSize="10" 
                                                Foreground="Gray" 
                                                FontWeight="Bold"
                                                Text="Кол-во файлов/сборок:"/>
                                            <Run FontSize="10" 
                                                Foreground="Gray" 
                                                Text="{Binding SettingCountItem}"/>
                                        </TextBlock.Inlines>
                                    </TextBlock>
                                </StackPanel>

                            </Grid>
                        </DataTemplate>
                    </ItemsControl.ItemTemplate>
                </ItemsControl>
            </StackPanel>
        </ScrollViewer>
        <!--Блок с набором конфигов-->

        <!--Нижний блок кнопок-->
        <Grid Grid.Row="3">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="0.2*"/>
                <ColumnDefinition Width="0.8*"/>
            </Grid.ColumnDefinitions>
            <StackPanel 
                Grid.Column="0"
                Orientation="Horizontal"
                HorizontalAlignment="Left">
                <Button 
                    x:Name="btnRun"
                    Content="▶"
                    Style="{StaticResource UserActionBtns}"
                    Foreground="Green"
                    IsEnabled="False"
                    ToolTip="Запустить выбранные конфигурации"
                    Click="OnBtnRun"/>
            </StackPanel>
            <StackPanel 
                Grid.Column="1"
                Orientation="Horizontal"
                HorizontalAlignment="Right">
                <Button 
                    x:Name="btnAddConf"
                    Content="➕"
                    Style="{StaticResource UserActionBtns}"
                    Foreground="Green"
                    ToolTip="Добавить новую конфигурацию для проекта"
                    IsEnabled="True"
                    Click="OnBtnAddConf"/>
                <Button 
                    x:Name="btnDelConf"
                    Content="✖️"
                    Style="{StaticResource UserActionBtns}"
                    Foreground="Red"
                    IsEnabled="False"
                    ToolTip="Удалить выбранные конфигурации"
                    Click="OnBtnDelConf"/>
            </StackPanel>
        </Grid>
        <!--Нижний блок кнопок-->
        
    </Grid>
</Window>
