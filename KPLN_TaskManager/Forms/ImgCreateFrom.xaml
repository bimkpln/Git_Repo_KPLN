<Window 
    x:Class="KPLN_TaskManager.Forms.ImgCreateFrom"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
    xmlns:local="clr-namespace:KPLN_TaskManager.Forms"
    mc:Ignorable="d" 
    Width="500"
    Height="Auto"
    SizeToContent="Height"
    Topmost="True"
    Background="White">

    <Window.Resources>

        <Style x:Key="TXTBX" TargetType="TextBox">
            <Setter Property="Foreground" Value="Black"/>
            <Setter Property="Margin" Value="5,0,5,0"/>
            <Setter Property="TextWrapping" Value="Wrap"/>
            <Setter Property="MinWidth" Value="400"/>
        </Style>

        <Style x:Key="RunBTN" TargetType="Button">
            <Setter Property="Background" Value="Orange"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="Margin" Value="2"/>
            <Setter Property="Height" Value="20"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="{x:Type Button}">
                        <Border CornerRadius="2" 
                                Background="{TemplateBinding Background}">
                            <Grid>
                                <ContentPresenter 
                                    x:Name="MyContentPresenter" 
                                    Content="{TemplateBinding Content}" 
                                    HorizontalAlignment="Center" 
                                    VerticalAlignment="Center"/>
                            </Grid>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background" Value="Red" />
                                <Setter Property="Foreground" Value="White" />
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter Property="Background" Value="#FF00C867" />
                                <Setter Property="Foreground" Value="White" />
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="CHCBX" TargetType="CheckBox">
            <Setter Property="Foreground" Value="Black"/>
            <Setter Property="Margin" Value="5"/>
        </Style>

    </Window.Resources>

    <Grid>
        <StackPanel>
            <InkCanvas 
                Name="inkCanvas" 
                Background="White" 
                PreviewMouseLeftButtonDown="InkCanvas_MouseLeftButtonDown"/>
            <StackPanel 
                Orientation="Horizontal" 
                HorizontalAlignment="Center" 
                VerticalAlignment="Top" 
                Margin="10">
                <Button 
                    Name="btnCapture" 
                    Content="Создать скрин"
                    Style="{StaticResource RunBTN}" 
                    Click="BtnCapture_Click"/>
                <Button 
                    Name="btnMarker" 
                    Content="Включить маркер" 
                    Style="{StaticResource RunBTN}" 
                    Click="EnableMarkerMode_Click"/>
                <Button 
                    Name="btnText" 
                    Content="Добавить текст" 
                    Style="{StaticResource RunBTN}" 
                    Click="EnableTextMode_Click"/>
                <Button 
                    Name="btnSave" 
                    Content="Прикрепить к заданию" 
                    Style="{StaticResource RunBTN}" 
                    Click="BtnSave_Click"/>
            </StackPanel>
        </StackPanel>
    </Grid>
</Window>
