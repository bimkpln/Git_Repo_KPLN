<Window x:Class="KPLN_Quantificator.Forms.StatusChangeClash"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="KPLN. Смена статусов коллизий"
        Height="250" Width="640"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize">
    <Grid Margin="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="150"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <Grid Grid.Row="2">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="1*"/>
                <ColumnDefinition Width="16"/>
                <ColumnDefinition Width="1*"/>
            </Grid.ColumnDefinitions>

            <StackPanel Grid.Column="0">
                <TextBlock Text="Статус был:" Margin="0,0,0,4" Opacity="0.75"/>
                <ComboBox x:Name="cbOldStatus" Height="24"/>
            </StackPanel>

            <StackPanel Grid.Column="2">
                <TextBlock Text="Статус станет:" Margin="0,0,0,4" Opacity="0.75"/>
                <ComboBox x:Name="cbNewStatus" Height="24"/>
            </StackPanel>
        </Grid>

        <StackPanel Grid.Row="4" Orientation="Horizontal" HorizontalAlignment="Right">
            <Button Content="Отмена" Width="110" Margin="0,0,8,0" Click="Cancel_Click"/>
            <Button Content="Применить" Width="130" IsDefault="True" Click="Apply_Click"/>
        </StackPanel>
    </Grid>
</Window>
