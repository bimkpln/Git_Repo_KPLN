<Window x:Class="KPLN_CoordiantorAI.Forms.ExternalModelWindow"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:KPLN_CoordiantorAI.Forms"
             mc:Ignorable="d" 
             d:DesignHeight="450"
             d:DesignWidth="800"
             Title="Работа с моделью"
             Height="620"
             Width="900"
             MinHeight="480"
             MinWidth="680"
             WindowStartupLocation="CenterOwner"
             Background="#FF20242D"
             FontFamily="Segoe UI">
    <Window.Resources>
        <Style TargetType="Button">
            <Setter Property="MinHeight" Value="30"/>
            <Setter Property="Padding" Value="12,4"/>
            <Setter Property="Background" Value="#FF2C6BED"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="BorderBrush" Value="#FF2C6BED"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="ButtonBorder"
                                Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Center"
                                              VerticalAlignment="Center"
                                              RecognizesAccessKey="True"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="ButtonBorder" Property="Background" Value="#FF4B83F3"/>
                                <Setter TargetName="ButtonBorder" Property="BorderBrush" Value="#FF7AA6FF"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter TargetName="ButtonBorder" Property="Opacity" Value="0.86"/>
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter TargetName="ButtonBorder" Property="Opacity" Value="0.55"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
        <Style TargetType="TextBox">
            <Setter Property="Background" Value="#FF2A303B"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="BorderBrush" Value="#FF4B5568"/>
            <Setter Property="CaretBrush" Value="White"/>
        </Style>
    </Window.Resources>
    <Grid Background="#FF20242D">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Заголовок -->
        <Border Grid.Row="0"
                Background="#FF262B35"
                BorderBrush="#FF343B49"
                BorderThickness="0,0,0,1"
                Padding="12,10">
            <TextBlock x:Name="TitleTextBlock"
                       Text="Работа с моделью"
                       FontSize="16"
                       FontWeight="SemiBold"
                       Foreground="White"/>
        </Border>

        <!-- История чата -->
        <ScrollViewer Grid.Row="1" 
                      x:Name="ChatScrollViewer"
                      VerticalScrollBarVisibility="Auto"
                      Margin="10">
            <StackPanel x:Name="ChatHistory"/>
        </ScrollViewer>

        <!-- Поле ввода -->
        <Grid Grid.Row="2" Margin="10,0,10,10">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>

            <TextBox x:Name="InputTextBox"
                     Grid.Column="0"
                     Margin="0,0,10,0"
                     Padding="10"
                     VerticalContentAlignment="Center"
                     AcceptsReturn="True"
                     MinHeight="40"
                     FontSize="14"
                     Foreground="White"
                     Background="#FF2A303B"
                     BorderBrush="#FF4B5568"
                     CaretBrush="White"/>

            <Button x:Name="SendButton"
                    Grid.Column="1"
                    Content="Отправить"
                    Padding="15,10"
                    MinWidth="100"
                    Margin="0,0,0,0"/>
        </Grid>
    </Grid>
</Window>
