<Window x:Class="KPLN_ModelChecker_Lib.Forms.CheckMEPHeight_ARDataForm"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
        xmlns:local="clr-namespace:KPLN_ModelChecker_Lib.Forms"
        mc:Ignorable="d" 
        SizeToContent="Height"
        Width="1075"
        Height="400"
        MaxHeight="750"
        ResizeMode="NoResize"
        Topmost="True"
        WindowStartupLocation="CenterOwner">
    <Window.Resources>
        <Style x:Key="MainButtons" TargetType="{x:Type Button}">
            <Setter Property="Height" Value="30"/>
            <Setter Property="Width" Value="250"/>
            <Setter Property="FontSize" Value="12"/>
            <Setter Property="Margin" Value="5,5,5,5"/>
        </Style>

        <Style x:Key="ItemButtons" TargetType="{x:Type Button}">
            <Setter Property="HorizontalAlignment" Value="Stretch"/>
            <Setter Property="VerticalAlignment" Value="Stretch"/>
            <Setter Property="Margin" Value="5,5,5,5"/>
        </Style>

        <Style x:Key="MainTBlock" TargetType="{x:Type TextBlock}">
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="Margin" Value="5"/>
            <Setter Property="TextWrapping" Value="Wrap"/>
            <Setter Property="FontWeight" Value="Bold"/>
            <Setter Property="VerticalAlignment" Value="Center"/>
            <Setter Property="HorizontalAlignment" Value="Center"/>
        </Style>

        <Style x:Key="TBlock" TargetType="{x:Type TextBlock}">
            <Setter Property="FontSize" Value="12"/>
            <Setter Property="Margin" Value="5"/>
            <Setter Property="TextWrapping" Value="Wrap"/>
            <Setter Property="VerticalAlignment" Value="Center"/>
            <Setter Property="HorizontalAlignment" Value="Left"/>
        </Style>

        <Style x:Key="TBox" TargetType="{x:Type TextBox}">
            <Setter Property="FontSize" Value="12"/>
            <Setter Property="Margin" Value="25,5,25,5"/>
            <Setter Property="TextWrapping" Value="Wrap"/>
            <Setter Property="TextAlignment" Value="Center"/>
            <Setter Property="VerticalAlignment" Value="Center"/>
            <Setter Property="HorizontalAlignment" Value="Stretch"/>
        </Style>

    </Window.Resources>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="0.85*"/>
            <RowDefinition Height="0.15*"/>
        </Grid.RowDefinitions>

        <Grid
            Grid.Row="0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="100"/>
                <ColumnDefinition Width="350"/>
                <ColumnDefinition Width="200"/>
                <ColumnDefinition Width="200"/>
                <ColumnDefinition Width="200"/>
            </Grid.ColumnDefinitions>
            <Grid.RowDefinitions>
                <RowDefinition Height="0.15*"/>
                <RowDefinition Height="0.85*"/>
            </Grid.RowDefinitions>

            <TextBlock 
                 Grid.Column="0"
                 Text="Выполнять проверку?" 
                 Style="{StaticResource MainTBlock}"/>
            <TextBlock 
                Grid.Column="1"
                Text="Имя помещения" 
                Style="{StaticResource MainTBlock}"/>
            <TextBlock 
                Grid.Column="2"
                Text="Назначение помещения" 
                Style="{StaticResource MainTBlock}"/>
            <TextBlock 
                Grid.Column="3"
                Text="Минимальная высота начала проверки" 
                Style="{StaticResource MainTBlock}"/>
            <TextBlock 
                Grid.Column="4"
                Text="Минимальная высота размещения" 
                Style="{StaticResource MainTBlock}"/>

            <ScrollViewer 
                x:Name="ScrollViewer" 
                Grid.Row="1"
                Grid.ColumnSpan="5"
                PreviewMouseWheel="ScrollViewer_PreviewMouseWheel">

                <ItemsControl x:Name="ARDataItemContr">
                    <ItemsControl.ItemTemplate>
                        <DataTemplate>
                            <Grid>
                                <Grid.RowDefinitions>
                                    <RowDefinition/>
                                    <RowDefinition/>
                                </Grid.RowDefinitions>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="100"/>
                                    <ColumnDefinition Width="350"/>
                                    <ColumnDefinition Width="200"/>
                                    <ColumnDefinition Width="200"/>
                                    <ColumnDefinition Width="200"/>
                                </Grid.ColumnDefinitions>

                                <CheckBox 
                                    Grid.Column="0"
                                    IsChecked="{Binding VMIsCheckRun}"
                                    VerticalAlignment="Center"
                                    HorizontalAlignment="Center"/>

                                <TextBlock 
                                    Grid.Column="1"
                                    Text="{Binding VMCurrentRoomName}" 
                                    Style="{StaticResource TBlock}"/>

                                <TextBlock 
                                    Grid.Column="2"
                                    Text="{Binding VMCurrentRoomDepartmentName}" 
                                    Style="{StaticResource TBlock}"/>

                                <TextBox 
                                    Grid.Column="3"
                                    Text="{Binding VMCurrentRoomMinElemElevationForCheck}" 
                                    Style="{StaticResource TBox}"/>

                                <TextBox 
                                    Grid.Column="4"
                                    Text="{Binding VMCurrentRoomMinDistance}" 
                                    Style="{StaticResource TBox}"/>

                                <GridSplitter 
                                    Grid.Row="1" 
                                    Height="3" 
                                    Margin="0,0,-50,0"
                                    Grid.ColumnSpan ="5" 
                                    VerticalAlignment="Bottom" 
                                    HorizontalAlignment="Stretch"/>
                            </Grid>
                        </DataTemplate>
                    </ItemsControl.ItemTemplate>
                </ItemsControl>
            </ScrollViewer>
        </Grid>

        <DockPanel 
            Grid.Row="1"
            HorizontalAlignment="Center"
            VerticalAlignment="Center">

            <Button 
                x:Name="SaveConfigBtn" 
                Content="Записать конфигурацию" 
                Click="SaveConfigBtn_Click"
                Width="150"
                BorderBrush="Orange"
                BorderThickness="2"
                Style="{StaticResource MainButtons}"/>

            <Button 
                x:Name="StartBtn" 
                Content="Выполнить" 
                Click="StartBtn_Click"
                Width="75"
                BorderBrush="Green"
                BorderThickness="2"
                Style="{StaticResource MainButtons}"/>
        </DockPanel>

    </Grid>
</Window>
