<Window x:Class="KPLN_Library_Forms.UI.ElementSinglePick"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
        xmlns:local="clr-namespace:KPLN_Library_Forms.UI"
        mc:Ignorable="d" 
        MinWidth="400" 
        MinHeight="400"
        MaxHeight="750"
        MaxWidth="750"
        ResizeMode="CanResizeWithGrip" 
        SizeToContent="WidthAndHeight" 
        Title="KPLN: Окно выбора"
        FontFamily="Monaco, Consolas, 'Andale Mono', 'DejaVu Sans Mono', monospace"
        Background="#2B2F38"
        WindowStyle="ToolWindow"
        WindowStartupLocation="CenterScreen" 
        Loaded="OnLoaded">
    <Window.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="LibStyles.xaml" />
            </ResourceDictionary.MergedDictionaries>
        </ResourceDictionary>
    </Window.Resources>

    <Grid>

        <Grid.RowDefinitions>
            <RowDefinition Height="35"/>
            <RowDefinition/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition></ColumnDefinition>
        </Grid.ColumnDefinitions>

        <DockPanel Background="#333840" LastChildFill="True">
            <TextBlock
                Text="Поиск:"
                Foreground="#EDEDED"
                VerticalAlignment="Center"
                FontWeight="Bold" 
                Margin="5"/>
            <TextBox 
                x:Name="SearchText"
                Style="{StaticResource KPLN_TextBox}"
                Grid.Row="0" 
                TextChanged="SearchText_Changed"
                TextAlignment="Left" 
                VerticalAlignment="Center" 
                DockPanel.Dock="Right" 
                FontStretch="ExtraExpanded"/>
        </DockPanel>

        <GridSplitter 
            Grid.Row="0" 
            Height="5" 
            Grid.ColumnSpan ="1" 
            VerticalAlignment="Bottom" 
            HorizontalAlignment="Stretch"/>

        <ScrollViewer 
            Grid.Row="1" 
            Margin="0, 0, 0, 5"
            HorizontalScrollBarVisibility="Disabled" 
            VerticalScrollBarVisibility="Visible">
            <StackPanel Orientation="Vertical">
                <ItemsControl x:Name="Elements">
                    <ItemsControl.ItemTemplate>
                        <DataTemplate>
                            <Button 
                                Style="{StaticResource KPLN_ElementPickButton}"
                                ToolTip="{Binding Tooltip}" 
                                Click="OnElementClick">
                                <TextBlock
                                    TextWrapping="Wrap"
                                    TextAlignment="Center"
                                    Text="{Binding Name}"/>
                            </Button>
                        </DataTemplate>
                    </ItemsControl.ItemTemplate>
                </ItemsControl>
            </StackPanel>
        </ScrollViewer>

        <GridSplitter 
            Grid.Row="1" 
            Height="5" 
            Grid.ColumnSpan ="1" 
            VerticalAlignment="Bottom" 
            HorizontalAlignment="Stretch"/>
    </Grid>
</Window>
