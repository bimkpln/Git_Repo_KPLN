<Window x:Class="KPLN_Library_Forms.UI.ButtonToRun"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
        xmlns:local="clr-namespace:KPLN_Library_Forms.UI"
        mc:Ignorable="d" 
        Width="300"  
        Height="200"
        MaxHeight="750"
        Title="KPLN: Окно выбора" 
        FontFamily="Monaco, Consolas, 'Andale Mono', 'DejaVu Sans Mono', monospace" 
        Background="#2B2F38"
        WindowStyle="ToolWindow" 
        Topmost="False" 
        WindowStartupLocation="CenterScreen" 
        SizeToContent="WidthAndHeight">
    <Window.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="Styles.xaml" />
            </ResourceDictionary.MergedDictionaries>
        </ResourceDictionary>
    </Window.Resources>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock 
            x:Name="MainDescription" 
            Grid.Row="0"
            Text="Задаётся через конструктор"
            Style="{StaticResource KPLN_TextBlock}"/>

        <ScrollViewer 
            Grid.Row="1" 
            Margin="0, 0, 0, 5"
            VerticalScrollBarVisibility="Visible">
            <StackPanel Orientation="Vertical">
                <ItemsControl x:Name="BtnColl">
                    <ItemsControl.ItemTemplate>
                        <DataTemplate>
                            <Button 
                                Style="{StaticResource KPLN_ButtonToRunItemButton}"
                                Content="{Binding Name}" 
                                ToolTip="{Binding Tooltip}" 
                                Click="OnBtnClick"/>
                        </DataTemplate>
                    </ItemsControl.ItemTemplate>
                </ItemsControl>
            </StackPanel>
        </ScrollViewer>
    </Grid>
</Window>
