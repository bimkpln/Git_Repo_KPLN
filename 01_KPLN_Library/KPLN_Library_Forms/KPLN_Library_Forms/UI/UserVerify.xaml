<Window x:Class="KPLN_Library_Forms.UI.UserVerify"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
        xmlns:local="clr-namespace:KPLN_Library_Forms.UI"
        mc:Ignorable="d"
        Title="KPLN: Авторизация специалиста BIM-отдела" 
        FontFamily="Monaco, Consolas, 'Andale Mono', 'DejaVu Sans Mono', monospace" 
        Background="#2B2F38"
        ResizeMode="NoResize"
        SizeToContent="WidthAndHeight" 
        WindowStyle="ToolWindow"  
        Topmost="False" 
        WindowStartupLocation="CenterScreen" 
        Loaded="OnLoaded">
    
    <Window.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="Styles.xaml" />
            </ResourceDictionary.MergedDictionaries>
        </ResourceDictionary>
    </Window.Resources>

    <Grid>
        
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="25"/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="350"/>
        </Grid.ColumnDefinitions>

        <DockPanel 
            Background="#333840"
            LastChildFill="True">
            <TextBlock 
                Text="Почему это появилось?" 
                Foreground="#EDEDED"
                VerticalAlignment="Center" 
                FontWeight="Bold" 
                Margin="5" 
                DockPanel.Dock="Top"/>
            <TextBlock 
                x:Name="FormDescription" 
                Text="Описание, которое устанавливается через конструктор" 
                Foreground="#EDEDED"
                TextWrapping="Wrap" 
                VerticalAlignment="Center" 
                Margin="5"
                DockPanel.Dock="Top"/>
            <TextBlock 
                Text="Пароль:" 
                Foreground="#EDEDED"
                VerticalAlignment="Center" 
                FontWeight="Bold" 
                Margin="5"/>
            <PasswordBox 
                x:Name="SearchPassword" 
                Grid.Row="0" 
                Style="{StaticResource KPLN_PasswordBox}"
                Margin="0,10,10,10"
                VerticalAlignment="Center" 
                DockPanel.Dock="Right" 
                FontStretch="ExtraExpanded"
                PasswordChanged="PasswordText_Changed"/>
        </DockPanel>

        <GridSplitter 
            Grid.Row="0" 
            Height="5"
            Grid.ColumnSpan ="1"
            VerticalAlignment="Bottom" 
            HorizontalAlignment="Stretch"/>

        <Button 
            x:Name="Run" 
            Grid.Row="1" 
            Style="{StaticResource KPLN_RunButton}"
            Content="Выполнить команду" 
            Click="OnRunClick"/>
    
    </Grid>
</Window>
