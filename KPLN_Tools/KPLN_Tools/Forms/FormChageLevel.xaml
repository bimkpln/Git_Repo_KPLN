<Window x:Class="KPLN_Tools.Forms.FormChageLevel"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        mc:Ignorable="d"
        ResizeMode="NoResize"
        Width="1250"
        Height="760"
        Title="Смещение элементов по уровню"
        WindowStartupLocation="CenterScreen">

    <Window.Resources>
        <Style TargetType="{x:Type RichTextBox}">
            <Setter Property="FocusVisualStyle" Value="{x:Null}" />
        </Style>

        <Style x:Key="OffsetTextBoxStyle" TargetType="{x:Type TextBox}">
            <Setter Property="Margin" Value="0,4,0,0"/>
            <Setter Property="Height" Value="24"/>
            <Setter Property="VerticalContentAlignment" Value="Center"/>
            <Setter Property="Padding" Value="5,0,5,0"/>
        </Style>
    </Window.Resources>

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="12"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="12"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="560"/>
            <ColumnDefinition Width="20"/>
            <ColumnDefinition Width="610"/>
        </Grid.ColumnDefinitions>

        <Grid Grid.Row="0" Grid.Column="0" Grid.ColumnSpan="3">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>

            <StackPanel Grid.Column="0" Orientation="Horizontal">
                <Label Content="Уровень:" VerticalAlignment="Center" Padding="0" Margin="0,0,10,0"/>
                <ComboBox x:Name="LevelSelector"
                          Width="350"
                          Height="24"
                          DisplayMemberPath="Name"
                          SelectionChanged="LevelSelector_SelectionChanged"/>
            </StackPanel>

            <Button Grid.Column="1"
                    Width="170"
                    Height="28"
                    Margin="10,0,0,0"
                    HorizontalAlignment="Right"
                    VerticalAlignment="Center"
                    Content="Копировать значения"
                    Click="Button_ClickCopyValues"/>
        </Grid>

        <GroupBox Grid.Row="2" Grid.Column="0" Header="Элементы выбранного уровня" Padding="8" FontSize="11">
            <ScrollViewer VerticalScrollBarVisibility="Auto">
                <RichTextBox x:Name="ElementList"
                             IsReadOnly="True"
                             Background="WhiteSmoke"
                             BorderThickness="0"
                             Padding="8,5,0,10"
                             FontSize="11"/>
            </ScrollViewer>
        </GroupBox>

        <GroupBox Grid.Row="2" Grid.Column="2" Header="Смещения по категориям" Padding="8" FontSize="11">
            <ScrollViewer VerticalScrollBarVisibility="Auto">
                <ItemsControl x:Name="CategoryItemsControl" ItemsSource="{Binding VisibleCategoryItems}">
                    <ItemsControl.ItemsPanel>
                        <ItemsPanelTemplate>
                            <StackPanel Orientation="Vertical"/>
                        </ItemsPanelTemplate>
                    </ItemsControl.ItemsPanel>

                    <ItemsControl.ItemTemplate>
                        <DataTemplate>
                            <Border BorderBrush="#FFBEBEBE"
                                    BorderThickness="1"
                                    CornerRadius="4"
                                    Margin="0,0,0,8"
                                    Padding="10"
                                    Background="#FFF9F9F9"
                                    VerticalAlignment="Top"
                                    HorizontalAlignment="Stretch">
                                <StackPanel>
                                    <TextBlock FontWeight="Bold" TextWrapping="Wrap">
                                        <Run Text="{Binding DisplayName}"/>
                                        <Run Text=" ("/>
                                        <Run Text="{Binding Count}"/>
                                        <Run Text=")"/>
                                    </TextBlock>

                                    <StackPanel Margin="0,10,0,0"
                                                Visibility="{Binding FirstOffsetVisibility}">
                                        <TextBlock Text="{Binding FirstOffsetLabel}"/>
                                        <TextBox Text="{Binding FirstOffsetText, UpdateSourceTrigger=PropertyChanged}"
                                                 Style="{StaticResource OffsetTextBoxStyle}"/>
                                    </StackPanel>
                                </StackPanel>
                            </Border>
                        </DataTemplate>
                    </ItemsControl.ItemTemplate>
                </ItemsControl>
            </ScrollViewer>
        </GroupBox>

        <Button Grid.Row="4"
                Grid.Column="2"
                Width="260"
                Height="28"
                HorizontalAlignment="Right"
                Background="#FFFFE7E7"
                Content="Сместить элементы"
                Click="Button_ClickApplyOffsets"/>
    </Grid>
</Window>