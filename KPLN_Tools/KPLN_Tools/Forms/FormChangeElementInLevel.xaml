<Window x:Class="KPLN_Tools.Forms.FormChangeElementInLevel"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d"
        Title="Перенос элементов между уровнями"
        Height="720"
        Width="1250"
        MinHeight="600"
        MinWidth="1000"
        WindowStartupLocation="CenterScreen"
        ResizeMode="CanResize">

    <Grid Margin="12">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="12"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="12"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="8"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <Grid Grid.Row="0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="20"/>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="20"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <StackPanel Grid.Column="0">
                <TextBlock Text="Исходный уровень"
                           FontWeight="SemiBold"
                           Margin="0,0,0,4"/>
                <ComboBox x:Name="CbSourceLevel"
                          Height="24"
                          SelectionChanged="CbSourceLevel_SelectionChanged"
                          DisplayMemberPath="Name"/>
            </StackPanel>

            <Button x:Name="BtnMove"
                    Grid.Column="2"
                    Width="170"
                    Height="34"
                    VerticalAlignment="Bottom"
                    Content="Перенести"
                    Click="BtnMove_Click"/>

            <StackPanel Grid.Column="4">
                <TextBlock Text="Целевой уровень"
                           FontWeight="SemiBold"
                           Margin="0,0,0,4"/>
                <ComboBox x:Name="CbTargetLevel"
                          Height="24"
                          SelectionChanged="CbTargetLevel_SelectionChanged"
                          DisplayMemberPath="Name"/>
            </StackPanel>
        </Grid>

        <Grid Grid.Row="2">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="16"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <Border Grid.Column="0"
                    BorderBrush="#D0D0D0"
                    BorderThickness="1"
                    CornerRadius="4"
                    Padding="10">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="6"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="8"/>
                        <RowDefinition Height="*"/>
                    </Grid.RowDefinitions>

                    <TextBlock Grid.Row="0"
                               Text="Элементы на исходном уровне"
                               FontSize="14"
                               FontWeight="SemiBold"/>

                    <TextBlock x:Name="TbSourceSummary"
                               Grid.Row="2"
                               Text="Групп: 0 | Элементов: 0"
                               FontWeight="SemiBold"/>

                    <ListBox x:Name="LbSourceElements"
                             Grid.Row="4"
                             HorizontalContentAlignment="Stretch"
                             ScrollViewer.VerticalScrollBarVisibility="Auto"
                             ScrollViewer.HorizontalScrollBarVisibility="Auto">
                        <ListBox.ItemTemplate>
                            <DataTemplate>
                                <TextBlock TextWrapping="Wrap">
                                    <Run Text="{Binding MainText, Mode=OneWay}" />
                                    <Run Text="{Binding GroupSuffix, Mode=OneWay}"
                                         Foreground="Blue" />
                                </TextBlock>
                            </DataTemplate>
                        </ListBox.ItemTemplate>
                    </ListBox>
                </Grid>
            </Border>

            <Border Grid.Column="2"
                    BorderBrush="#D0D0D0"
                    BorderThickness="1"
                    CornerRadius="4"
                    Padding="10">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="6"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="8"/>
                        <RowDefinition Height="*"/>
                    </Grid.RowDefinitions>

                    <TextBlock Grid.Row="0"
                               Text="Элементы на целевом уровне"
                               FontSize="14"
                               FontWeight="SemiBold"/>

                    <TextBlock x:Name="TbTargetSummary"
                               Grid.Row="2"
                               Text="Групп: 0 | Элементов: 0"
                               FontWeight="SemiBold"/>

                    <ListBox x:Name="LbTargetElements"
                             Grid.Row="4"
                             HorizontalContentAlignment="Stretch"
                             ScrollViewer.VerticalScrollBarVisibility="Auto"
                             ScrollViewer.HorizontalScrollBarVisibility="Auto">
                        <ListBox.ItemTemplate>
                            <DataTemplate>
                                <TextBlock TextWrapping="Wrap">
                                    <Run Text="{Binding MainText, Mode=OneWay}" />
                                    <Run Text="{Binding GroupSuffix, Mode=OneWay}"
                                         Foreground="Blue" />
                                </TextBlock>
                            </DataTemplate>
                        </ListBox.ItemTemplate>
                    </ListBox>
                </Grid>
            </Border>
        </Grid>

        <Border Grid.Row="4"
                BorderBrush="#D0D0D0"
                BorderThickness="1"
                CornerRadius="4"
                Padding="10,8">
            <TextBlock x:Name="TbStatus"
                       Text="Готов к запуску"
                       VerticalAlignment="Center"/>
        </Border>

        <ProgressBar x:Name="PbProgress"
                     Grid.Row="6"
                     Height="20"
                     Minimum="0"
                     Maximum="1"
                     Value="0"/>
    </Grid>
</Window>