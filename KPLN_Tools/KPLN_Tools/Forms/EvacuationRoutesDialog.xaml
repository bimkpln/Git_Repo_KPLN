<Window x:Class="KPLN_Tools.ExternalCommands.UI.EvacuationRoutesDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="KPLN. Маршруты эвакуации"
        Width="1280"
        Height="720"
        MinWidth="1100"
        MinHeight="560"
        WindowStartupLocation="CenterOwner"
        ResizeMode="CanResize"
        FontFamily="Century Gothic">
    <Grid Margin="14">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="10"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="10"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="10"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <Grid Grid.Row="0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="90"/>
                <ColumnDefinition Width="18"/>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="90"/>
                <ColumnDefinition Width="18"/>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <TextBlock Grid.Column="0" VerticalAlignment="Center" Text="Высота мм:"/>
            <TextBox Grid.Column="1" x:Name="TbHeightMm" Margin="6,0,0,0" Height="26" VerticalContentAlignment="Center" PreviewTextInput="DigitsOnly_PreviewTextInput" DataObject.Pasting="DigitsOnly_OnPaste"/>

            <TextBlock Grid.Column="3" VerticalAlignment="Center" Text="Ширина мм:"/>
            <TextBox Grid.Column="4" x:Name="TbWidthMm" Margin="6,0,0,0" Height="26" VerticalContentAlignment="Center" PreviewTextInput="DigitsOnly_PreviewTextInput" DataObject.Pasting="DigitsOnly_OnPaste"/>

            <CheckBox Grid.Column="6" x:Name="CbUseRunWidth" Content="По ширине марша" VerticalAlignment="Center" Margin="6,0,0,0" Checked="UseRunWidth_Changed" Unchecked="UseRunWidth_Changed" IsChecked="True"/>
        </Grid>

        <Grid Grid.Row="2">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="8"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <CheckBox Grid.Row="0" x:Name="CbAddToEvacuationWorkset" Content="Добавить в рабочий набор, содержащий «ЭВАКУАЦИИ»" Checked="EvacuationWorkset_Changed" Unchecked="EvacuationWorkset_Changed" IsChecked="True"/>
            <ComboBox Grid.Row="2" x:Name="CmbEvacuationWorksets" Height="26" SelectedValuePath="Id" Padding="6,0,4,0" VerticalContentAlignment="Center" HorizontalContentAlignment="Stretch">
                <ComboBox.ItemContainerStyle>
                    <Style TargetType="{x:Type ComboBoxItem}">
                        <Setter Property="VerticalContentAlignment" Value="Center"/>
                        <Setter Property="Padding" Value="6,3,4,3"/>
                    </Style>
                </ComboBox.ItemContainerStyle>
                <ComboBox.ItemTemplate>
                    <DataTemplate>
                        <TextBlock Text="{Binding Name}" VerticalAlignment="Center"/>
                    </DataTemplate>
                </ComboBox.ItemTemplate>
            </ComboBox>
        </Grid>

        <DataGrid Grid.Row="4"
                  x:Name="DgStairs"
                  AutoGenerateColumns="False"
                  CanUserAddRows="False"
                  CanUserDeleteRows="False"
                  IsReadOnly="False"
                  SelectionMode="Single"
                  SelectionUnit="FullRow"
                  HeadersVisibility="Column"
                  GridLinesVisibility="Horizontal"
                  SelectionChanged="DgStairs_SelectionChanged">
            <DataGrid.Columns>
                <DataGridTemplateColumn Header="Строить" Width="70">
                    <DataGridTemplateColumn.CellTemplate>
                        <DataTemplate>
                            <CheckBox IsChecked="{Binding IsIncluded, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </DataTemplate>
                    </DataGridTemplateColumn.CellTemplate>
                </DataGridTemplateColumn>
                <DataGridTemplateColumn Header="Статус" Width="155" IsReadOnly="True">
                    <DataGridTemplateColumn.CellTemplate>
                        <DataTemplate>
                            <StackPanel Orientation="Horizontal">
                                <Ellipse Width="11" Height="11" Fill="{Binding StatusBrush}" Margin="0,0,6,0" VerticalAlignment="Center"/>
                                <TextBlock Text="{Binding StatusText}" VerticalAlignment="Center"/>
                            </StackPanel>
                        </DataTemplate>
                    </DataGridTemplateColumn.CellTemplate>
                </DataGridTemplateColumn>
                <DataGridTextColumn Header="ID" Binding="{Binding ElementId}" Width="85" IsReadOnly="True"/>
                <DataGridTemplateColumn Header="Объект" Width="145" IsReadOnly="True">
                    <DataGridTemplateColumn.CellTemplate>
                        <DataTemplate>
                            <TextBlock Margin="{Binding ObjectIndent}" Text="{Binding ObjectText}" VerticalAlignment="Center"/>
                        </DataTemplate>
                    </DataGridTemplateColumn.CellTemplate>
                </DataGridTemplateColumn>
                <DataGridTextColumn Header="Имя" Binding="{Binding Name}" Width="160" IsReadOnly="True"/>
                <DataGridTextColumn Header="Тип" Binding="{Binding TypeName}" Width="330" IsReadOnly="True"/>
                <DataGridTextColumn Header="Элементы" Binding="{Binding ElementsText}" Width="130" IsReadOnly="True"/>
                <DataGridTextColumn Header="Уровни" Binding="{Binding NestedText}" Width="90" IsReadOnly="True"/>
            </DataGrid.Columns>
        </DataGrid>

        <Grid Grid.Row="6">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>

            <TextBlock Grid.Column="0" x:Name="TbStatus" Margin="0,0,12,0" HorizontalAlignment="Stretch" VerticalAlignment="Center" Text="Выберите строку или запустите обработку." TextWrapping="Wrap"/>

            <StackPanel Grid.Column="1" Orientation="Horizontal" HorizontalAlignment="Right">
                <Button x:Name="BtnPickAndBuild" Width="155" Height="26" Margin="0,0,8,0" Content="Выбрать и построить" Click="PickAndBuild_Click"/>
                <Button x:Name="BtnBuildAll" Width="180" Height="26" Margin="0,0,8,0" Content="Построить отмеченные" Click="BuildAll_Click"/>
                <Button x:Name="BtnSaveReport" Visibility="Collapsed" Width="130" Height="26" Margin="0,0,8,0" Content="Сохранить отчёт" Click="SaveReport_Click"/>
                <Button Width="80" Height="26" Content="Закрыть" Click="Close_Click"/>
            </StackPanel>
        </Grid>
    </Grid>
</Window>