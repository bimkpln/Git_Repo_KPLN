<Window x:Class="KPLN_Tools.Forms.ScheduleMainForm"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
        xmlns:local="clr-namespace:KPLN_Tools.Forms"
        mc:Ignorable="d" 
        SizeToContent="WidthAndHeight"
        MinHeight="300"
        MaxHeight="900"
        MinWidth="500"
        MaxWidth="1200"
        ResizeMode="NoResize"
        WindowStartupLocation="CenterOwner"
        Background="#FF292D36">

    <Window.InputBindings>
        <KeyBinding
            Key="Escape"
            Command="{Binding CloseWindowCmd}"
            CommandParameter="{Binding RelativeSource={RelativeSource AncestorType=Window}}" />
    </Window.InputBindings>

    <Window.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="Styles.xaml" />
            </ResourceDictionary.MergedDictionaries>
        </ResourceDictionary>
    </Window.Resources>

    <Border
        Style="{StaticResource RCTG}">
        <Grid Margin="10">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <!-- Загаловак -->
            <TextBlock 
                Grid.Row="0"
                Text="{Binding Title}"
                Style="{StaticResource TXT}"/>

            <!-- Табліца -->
            <DataGrid 
                x:Name="DG_Schedule"
                Grid.Row="1"
                ItemsSource="{Binding DataView}"
                AutoGenerateColumns="True"
                IsReadOnly="True"
                HeadersVisibility="All"
                CanUserAddRows="False"
                CanUserDeleteRows="False"
                CanUserReorderColumns="False"
                CanUserResizeRows="True"
                CanUserResizeColumns="True"
                CanUserSortColumns="False"
                AutoGeneratingColumn="DG_Schedule_AutoGeneratingColumn"
                MouseDoubleClick="DG_Schedule_MouseDoubleClick"/>

            <!-- Кнопкі -->
            <DockPanel 
                Grid.Row="2"
                HorizontalAlignment="Right"
                Margin="0,5,0,0">
                <Button 
                    Content="Перенести"
                    Style="{StaticResource BTN}"
                    Width="80"
                    Height="30"
                    Command="{Binding SetToRevitCmd}"
                    CommandParameter="{Binding RelativeSource={RelativeSource AncestorType=Window}}"/>
                <Button 
                    Content="Отмена"
                    Style="{StaticResource BTN}"
                    Width="80"
                    Height="30"
                    Command="{Binding CloseWindowCmd}"
                    CommandParameter="{Binding RelativeSource={RelativeSource AncestorType=Window}}"/>
            </DockPanel>
        </Grid>
    </Border>
</Window>
