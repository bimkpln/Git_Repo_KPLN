<UserControl x:Class="KPLN_Tools.Forms.FamilyManager"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             MinWidth="240" MinHeight="120" FontFamily="Century Gothic">
    <Grid Background="AliceBlue">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <Grid Grid.Row="0" Margin="12,10,12,0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>

            <TextBlock Grid.Column="0" TextWrapping="Wrap" VerticalAlignment="Center">
                <Run Text="Текущий отдел:" FontWeight="Bold"/>
                <Run x:Name="RunCurrentValue" Text="загрузка..."/>
            </TextBlock>

            <Button x:Name="BtnSettings" Grid.Column="1" Width="30" Height="24" HorizontalAlignment="Left" VerticalAlignment="Center" ToolTip="Настройки" Click="BtnSettings_Click">
                <TextBlock Text="&#xE713;" FontFamily="Segoe MDL2 Assets" FontSize="16" HorizontalAlignment="Center" VerticalAlignment="Center"/>
            </Button>
        </Grid>
      
        
        
        
        <Border x:Name="MainArea"
                Grid.Row="1"
                Margin="6"
                BorderBrush="#E0E0E0"
                BorderThickness="1"
                CornerRadius="6"
                Background="{x:Null}">
            <!-- сюда потом подставляешь контент: MainArea.Child = ... -->
        </Border>
        
        
        
        
    </Grid>
</UserControl>
