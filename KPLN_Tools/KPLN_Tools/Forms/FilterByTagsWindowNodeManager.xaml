<Window x:Class="KPLN_Tools.Forms.FilterByTagsWindowNodeManager"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="KPLN. Менеджер узлов. Фильтр по тегам"
        Height="355"
        Width="420"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize">
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="0" Orientation="Horizontal">
            <ComboBox x:Name="TagsComboBox" Width="340" Margin="0,0,8,0" ItemsSource="{Binding AvailableTags}"/>
            <Button Content="+" Width="30" Click="BtnAddTag_Click" FontSize="14" Background="#FFB6F1BC" FontWeight="Bold"/>
        </StackPanel>

        <Border Grid.Row="1" Margin="0,8,0,8" BorderThickness="1" BorderBrush="#DDD" CornerRadius="6" Padding="6">
            <ScrollViewer VerticalScrollBarVisibility="Auto">
                <ItemsControl ItemsSource="{Binding SelectedTags}">
                    <ItemsControl.ItemTemplate>
                        <DataTemplate>
                            <Border Margin="0,0,0,4" Padding="4" CornerRadius="4" BorderThickness="1" BorderBrush="#CCC" Background="#F7F7F7">
                                <StackPanel Orientation="Horizontal">
                                    <TextBlock Text="{Binding}" VerticalAlignment="Center" Margin="0,0,8,0"/>
                                    <Button Content="✕" Width="22" Height="22" Background="#FFFFB9B9" Click="RemoveTagButton_Click"/>
                                </StackPanel>
                            </Border>
                        </DataTemplate>
                    </ItemsControl.ItemTemplate>
                </ItemsControl>
            </ScrollViewer>
        </Border>

        <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Right" Height="24">
            <Button Content="Поиск по выбранным тегам" Width="180" Margin="0,0,8,0" Click="BtnSearch_Click"/>
            <Button Content="Отмена" Width="90" Click="BtnCancel_Click"/>
        </StackPanel>
    </Grid>
</Window>
