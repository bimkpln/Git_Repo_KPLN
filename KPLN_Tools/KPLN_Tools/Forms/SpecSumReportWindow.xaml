<Window x:Class="KPLN_Tools.Forms.SpecSumReportWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Сумма спецификаций"
        Height="720"
        Width="1100"
        WindowStartupLocation="CenterScreen"
        FontFamily="Segoe UI">
    <Grid Margin="14">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <TextBlock x:Name="HeaderTextBlock"
                   Grid.Row="0"
                   Margin="0,0,0,12"
                   FontSize="18"
                   FontWeight="SemiBold"
                   Text="Сумма спецификаций" />

        <Border Grid.Row="1"
                Background="White"
                BorderBrush="#D8E2EE"
                BorderThickness="1">
            <FlowDocumentScrollViewer x:Name="ReportViewer"
                                      IsToolBarVisible="False"
                                      VerticalScrollBarVisibility="Auto" />
        </Border>

        <StackPanel Grid.Row="2"
                    Margin="0,12,0,0"
                    Orientation="Horizontal"
                    HorizontalAlignment="Right">
            <Button x:Name="SaveButton"
                    MinWidth="130"
                    Height="30"
                    Margin="0,0,8,0"
                    Content="Сохранить TXT"
                    Click="SaveButton_Click" />
            <Button x:Name="SaveHtmlButton"
                    MinWidth="130"
                    Height="30"
                    Margin="0,0,8,0"
                    Content="Сохранить HTML"
                    Click="SaveHtmlButton_Click" />
            <Button MinWidth="90"
                    Height="30"
                    Content="Закрыть"
                    IsCancel="True" />
        </StackPanel>
    </Grid>
</Window>


