<Window x:Class="KPLN_ModelChecker_Batch.Forms.UserStringInfo"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
        xmlns:local="clr-namespace:KPLN_ModelChecker_Batch.Forms"
        mc:Ignorable="d" 
        FontFamily="Monaco, Consolas, 'Andale Mono', 'DejaVu Sans Mono', monospace"
        Title="KPLN: Сведения о файле" 
        Width="900" 
        SizeToContent="Height" 
        Background="#FF292D36" 
        WindowStartupLocation="CenterScreen" 
        ResizeMode="NoResize" 
        WindowStyle="ToolWindow" 
        Topmost="True">
    <Window.Resources>
        <Style TargetType="Button" x:Key="BTN">
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="Background" Value="{x:Null}"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="BorderBrush" Value="White"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Padding" Value="5"/>
        </Style>
        <Style TargetType="TextBlock" x:Key="TXT">
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="Margin" Value="5"/>
            <Setter Property="TextWrapping" Value="Wrap"/>
        </Style>
    </Window.Resources>

    <Grid Margin="5">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto"/>
            <ColumnDefinition/>
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <Grid Grid.Row="0" Grid.Column="1">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <TextBlock 
                x:Name="tbHeader" 
                TextWrapping="Wrap" 
                Margin="5" 
                Grid.Row="0" 
                Text="Информация о файле" 
                FontWeight="Bold" 
                FontSize="16" 
                Foreground="White"/>

            <TextBlock 
                Grid.Column="0"
                Grid.Row="1"
                FontSize="16"
                Text="Имя файла\папки:"
                Margin="5,0,5,0"
                Foreground="White"/>
            <TextBox 
                x:Name="InputName"
                Grid.Row="2"
                FontSize="16" 
                Text="{Binding UserInputName}"
                Margin="5">
            </TextBox>

            <TextBlock 
                Grid.Column="0"
                Grid.Row="3"
                FontSize="16"
                Text="Полный путь к файлу\папке:"
                Margin="5,0,5,0"
                Foreground="White"/>
            <TextBox 
                x:Name="InputPath"
                Grid.Row="4"
                FontSize="16" 
                Text="{Binding UserInputPath}"
                Margin="5">
            </TextBox>
        </Grid>

        <StackPanel 
            Grid.Column="0" 
            Grid.Row="1" 
            Grid.ColumnSpan="2" 
            Margin="5" 
            Orientation="Horizontal">
            <Button 
                x:Name="btnOk" 
                Style="{StaticResource BTN}" 
                Content="Принято" 
                Click="OnOk"/>
        </StackPanel>
    </Grid>
</Window>
