<Window x:Class="KPLN_ExtraFilter.Forms.SelectionByClickForm"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
        xmlns:local="clr-namespace:KPLN_ExtraFilter.Forms"
        mc:Ignorable="d" 
        WindowStyle="ToolWindow" 
        WindowStartupLocation="CenterOwner" 
        ResizeMode="NoResize" 
        SizeToContent="WidthAndHeight" 
        Topmost="True"
        FontFamily="Monaco, Consolas, 'Andale Mono', 'DejaVu Sans Mono', monospace"
        Title="KPLN: Выбор похожих" 
        Background="#FF292D36">
    <Window.Resources>

        <Style TargetType="Rectangle" x:Key="RCTG">
            <Setter Property="RadiusX" Value="2"/>
            <Setter Property="RadiusY" Value="2"/>
            <Setter Property="Margin" Value="2"/>
            <Setter Property="Fill" Value="AliceBlue"/>
            <Setter Property="Opacity" Value="0.1"/>
        </Style>

        <Style TargetType="Button" x:Key="BTN">
            <Setter Property="Background" Value="{x:Null}"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="BorderBrush" Value="White"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Margin" Value="5,2,5,2"/>
            <Setter Property="FontSize" Value="14"/>
        </Style>
        
        <Style TargetType="TextBlock" x:Key="TXT">
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="VerticalAlignment" Value="Center"/>
            <Setter Property="TextWrapping" Value="Wrap"/>
            <Setter Property="MaxWidth" Value="400"/>
            <Setter Property="Margin" Value="5,5,0,0"/>
            <Setter Property="FontSize" Value="14"/>
        </Style>
        
        <Style TargetType="CheckBox" x:Key="CHB">
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="Margin" Value="5,0,0,5"/>
            <Setter Property="FontSize" Value="14"/>
        </Style>

        <Style TargetType="ComboBox" x:Key="CBX">
            <Setter Property="Margin" Value="5,5,5,5"/>
            <Setter Property="FontSize" Value="14"/>
        </Style>

        <Style TargetType="RadioButton" x:Key="RB">
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="Margin" Value="5,3,0,3"/>
            <Setter Property="FontSize" Value="14"/>
        </Style>

    </Window.Resources>

    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="250"/>
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="25"/>
        </Grid.RowDefinitions>

        <Rectangle 
            Grid.Row="0"
            Style="{StaticResource RCTG}"/>
        <StackPanel
            Grid.Row="0">
            <TextBlock 
                Style="{StaticResource TXT}"
                FontSize="14"
                Text="Что выбираем?"
                Margin="5,5,5,5"/>
            <CheckBox 
                Style="{StaticResource CHB}"
                IsChecked="{Binding What_SameCategory, UpdateSourceTrigger=PropertyChanged, Mode=TwoWay}"
                Checked="CheckBox_Checked"
                Unchecked="CheckBox_Checked"
                Content="Одинаковой категории"/>
            <CheckBox 
                Style="{StaticResource CHB}"
                IsChecked="{Binding What_SameFamily, UpdateSourceTrigger=PropertyChanged}"
                Checked="CheckBox_Checked"
                Unchecked="CheckBox_Checked"
                Content="Одинакового семейства"/>
            <CheckBox 
                Style="{StaticResource CHB}"
                IsChecked="{Binding What_SameType, UpdateSourceTrigger=PropertyChanged}"
                Checked="CheckBox_Checked"
                Unchecked="CheckBox_Checked"
                Content="Одинакового типа"/>
            <CheckBox 
                x:Name="CHB_SameWorkset"
                Style="{StaticResource CHB}"
                IsEnabled="False"
                IsChecked="{Binding What_Workset, UpdateSourceTrigger=PropertyChanged}"
                Checked="CheckBox_Checked"
                Unchecked="CheckBox_Checked"
                Content="Одного рабочего набора"/>
            <CheckBox 
                x:Name="CHB_SameParameterData"
                Style="{StaticResource CHB}"
                IsChecked="{Binding What_ParameterData, UpdateSourceTrigger=PropertyChanged}"
                Checked="CheckBox_Checked"
                Unchecked="CheckBox_Checked">
                <TextBlock
                    Text="Одинакового значения пар-ра"
                    TextWrapping="Wrap"/>
            </CheckBox>
            <ComboBox 
                Name="CBX_SelectParameter"
                Style="{StaticResource CBX}"
                ItemsSource="{Binding SelectedElemParams, UpdateSourceTrigger=PropertyChanged}"
                SelectedItem="{Binding What_SelectedParam, UpdateSourceTrigger=PropertyChanged}"
                HorizontalAlignment="Stretch"
                IsEnabled="{Binding What_ParameterData}"
                ToolTip="Выбери параметр для поиска">
                <ComboBox.ItemTemplate>
                    <DataTemplate>
                        <TextBlock 
                            Text="{Binding CurrentParamName}"
                            Style="{StaticResource TXT}"
                            Foreground ="Black"
                            FontSize ="12"
                            Margin="5,0,0,5"/>
                    </DataTemplate>
                </ComboBox.ItemTemplate>
                <!--Чтобы при наведении на элемент всплывала падсказка (без этого - только при наведении на текст)-->
                <ComboBox.ItemContainerStyle>
                    <Style TargetType="ComboBoxItem">
                        <Setter Property="ToolTip" Value="{Binding CurrentToolTip, UpdateSourceTrigger=PropertyChanged}"/>
                    </Style>
                </ComboBox.ItemContainerStyle>
            </ComboBox>
        </StackPanel>

        <Rectangle 
            Grid.Row="1"
            Style="{StaticResource RCTG}"/>
        <StackPanel
            Grid.Row="1">
            <TextBlock 
                Style="{StaticResource TXT}"
                FontSize="14"
                Text="Где выбираем?"/>
            <RadioButton
                Style="{StaticResource RB}"
                GroupName="WhereSelection"
                IsChecked="{Binding Where_Model, UpdateSourceTrigger=PropertyChanged}"
                Content="Модель"/>
            <RadioButton
                Style="{StaticResource RB}"
                GroupName="WhereSelection"
                IsChecked="{Binding Where_CurrentView, UpdateSourceTrigger=PropertyChanged}"
                Content="Текущий вид"/>
        </StackPanel>

        <Rectangle 
            Grid.Row="2"
            Style="{StaticResource RCTG}"/>
            <StackPanel
                Grid.Row="2">
            <TextBlock 
                Style="{StaticResource TXT}"
                FontSize="14"
                Text="Исключения?"/>
            <CheckBox 
                Style="{StaticResource CHB}"
                IsChecked="{Binding Belong_Group, UpdateSourceTrigger=PropertyChanged}"
                Content="Эл-ты групп"/>
            </StackPanel>

        <DockPanel
            Grid.Row="4">
            <Button
                x:Name="RunBtn"
                Width="150"
                Style="{StaticResource BTN}"
                Content="Выбрать!"
                IsEnabled="False"
                Click="RunBtn_Click"
                ToolTip="Выбрать элементы согласно конфигурации"/>
            <Button
                x:Name="CleareConfigBtn"
                Style="{StaticResource BTN}"
                Content="Сбросить!"
                IsEnabled="False"
                Click="CleareConfigBtn_Click"
                ToolTip="Сбросить конфигурацию последнего запука"/>
        </DockPanel>
    </Grid>
</Window>
